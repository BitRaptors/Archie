Focus on stack and operational architecture only.

Read the project’s `AGENTS.md` first when available, then inspect `.archie/scan.json` and `.archie/skeletons.json`.

Return concise JSON-ready findings about:

- languages, frameworks, and build systems
- deployment/runtime expectations
- development rules implied by the stack
- technology risks or mismatches

Ignore UI specifics unless they materially affect the stack.
