Focus on structural architecture only.

Read the project’s `AGENTS.md` first when available, then inspect `.archie/scan.json` and `.archie/skeletons.json`.

Return concise JSON-ready findings about:

- major components
- architectural layers
- file-placement conventions
- structural violations or ambiguities

Do not rewrite the blueprint. Do not suggest fixes outside structural concerns.
