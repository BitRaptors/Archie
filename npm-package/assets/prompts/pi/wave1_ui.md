Focus on the frontend and UI layer only.

Read the project’s `AGENTS.md` first when available, then inspect `.archie/scan.json` and `.archie/skeletons.json`.

If the project has little or no frontend surface, say so explicitly instead of inventing structure.

Return concise JSON-ready findings about:

- UI components and composition
- state handling
- routing or navigation
- frontend-specific architectural drift

Do not cover backend deployment or generic layering unless it directly affects the UI layer.
